---
title: "Serving & the admin UI"
description: "The HTTP API, entity point-reads, /sql, and the built-in admin UI."
order: 1
---

_This page is a stub in the first documentation pass — the IA is in place; content is being filled in._

The HTTP API, entity point-reads, /sql, and the built-in admin UI.
